apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo
spec:
  template:
    spec:
      containers:
      - name: labeler
        env:
        - name: MONGO_ADDRESS
          value: null
          valueFrom:
            secretKeyRef:
              name: mongo-auth
              key: uri
