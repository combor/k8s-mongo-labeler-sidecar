apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../base
patches:
- path: mongo.yaml
configMapGenerator:
- name: mongo-bootstrap
  behavior: merge
  files:
  - start-mongo.sh=../../start-mongo-auth.sh
generatorOptions:
  disableNameSuffixHash: true
