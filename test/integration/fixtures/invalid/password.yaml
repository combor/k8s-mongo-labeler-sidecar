apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo
spec:
  template:
    spec:
      containers:
      - name: labeler
        env:
        - name: MONGO_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongo-auth
              key: bad-password
